名称：
	文件上传

用法：
	sgFileupload['install']({
	    'target' : 'avatar',
	    'alias' : '头像',
	    'source' : jQuery('#mainForm\\:avatarHidden').val(),
	    'class' : 'attachment-extend--xxx'
	});

注意：
	class默认为preset
